{% extends '_layouts/cp' %}
{% set title = 'Expo Contact Form' %}
{% set viewTemplate = craft.app.request.getQueryParam('view') ?? 'home' %}
{% set pluginCpUrl = url('ecf') %}

{# {% set sidebar %}
    <nav>
		<ul id='groups'>
			<li>
				<a href="{{cpUrl('ecf/pages/home')}}" {% if selectedSubnavItem|default('') == 'home' %}class="sel"{% endif %}>Home</a>
			</li>
        </ul>
    </nav>
{% endset %} #}
